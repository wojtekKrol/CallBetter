export enum RouteTypes {
  INDEX = '/',
  CREATE_PROFILE = '/create-profile',
  HOME = '/home',
  PROFILE = '/profile',
  LOG_IN = '/log-in',
  SIGN_UP = '/sign-up',
  VIDEO_CALL = '/call/:callId'
}

export default RouteTypes;
